name: azar_chat_app
description: "Azar-style random video chat"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.0-290.1.beta

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_webrtc: ^0.14.0
  web_socket_channel: ^3.0.1
  permission_handler: ^11.3.1
  supabase_flutter: ^2.8.0
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
